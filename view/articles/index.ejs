<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet"
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
        crossorigin="anonymous">
    <style>
        html,
        body {
            height: 100%;
            margin: 0;
            background-image: url('https://i.pinimg.com/736x/8c/29/bb/8c29bb7954f08e2b99d6370e90e74a16.jpg'); /* Adjust path to your image */
            background-size: cover;
            background-position: center;
            display: flex;
            justify-content: center;
            align-items: center;
            padding-top: 40px; /* Adjust as needed */
            padding-bottom: 40px; /* Adjust as needed */
        }

        .container {
            background-color: rgba(189, 115, 58, 0.9); /* Background color for the container */
            padding: 20px;
            border-radius: 10px;
            max-width: 600px;
        }
    </style>
    <title>Blog Website</title>
</head>

<body class="text-center">
    <div class="container">
        <h1 class="mb-4">Welcome To Blog Website</h1>
        <a href="/articles/new" class="btn btn-success mb-4">Create Article</a>

        <% articles.forEach(article => { %>
        <div class="card mb-4 w-100">
            <div class="card-body">
                <h4 class="card-title"><%= article.title %></h4>
                <div class="card-subtitle text-muted mb-2">
                    <%= article.createdAt.toLocaleDateString() %>
                </div>
                <div class="card-text mb-2">
                    <%= article.description %>
                </div>
                <a href="articles/<%= article.slug %>" class="btn btn-primary me-2">Read More</a>
                <a href="articles/edit/<%= article.id %>" class="btn btn-info me-2">Edit</a>
                <form action="/articles/<%= article.id %>?_method=DELETE" method="POST" class="d-inline">
                    <button type="submit" class="btn btn-danger"
                        onclick="return confirm('Are you sure you want to delete this article?')">Delete</button>
                </form>
            </div>
        </div>
        <% }) %>
    </div>
</body>

</html>
